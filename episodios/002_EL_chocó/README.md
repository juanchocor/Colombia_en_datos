# 002 - EL Chocó

Este episodio agrupa las fuentes, datos, analisis, guiones y materiales de salida para trabajar el tema del Chocó en Colombia.

## Estructura

```text
002_EL_chocó/
├── README.md
├── requirements.txt
├── environment.yml
├── .gitignore
├── Data/
│   ├── raw/
│   ├── interim/
│   └── processed/
├── notebooks/
├── src/
├── outputs/
├── docs/
├── video/
└── LICENSE
```

## Datos disponibles

Los archivos fuente actuales se encuentran en `Data/raw/`.

## Proximo flujo sugerido

1. Revisar y documentar las fuentes y diccionario de variables.
2. Normalizar los datasets en `src/` o `notebooks/`.
3. Guardar versiones intermedias y limpias en `Data/interim/` y `Data/processed/`.
4. Exportar graficos, tablas y materiales visuales en `outputs/`, `docs/` y `video/`.
